import { HeaderContainer } from "./style";

export const Header = () => {
  return (
    <HeaderContainer>
      <h1>Header</h1>
      {/* here you need to add the profile component and the text on the left aswell */}
    </HeaderContainer>
  );
};

/** @type {import('next').NextConfig} */
const nextConfig = {
  compiler: {
    styledComponents: true,
  },
  //   experimental: {
  //     esmExternals: "loose",
  //   },
};

export default nextConfig;
